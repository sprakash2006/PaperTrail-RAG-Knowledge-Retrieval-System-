# Root .env — consumed by docker-compose for the FRONTEND build args.
# (Backend secrets live in backend/.env, not here.)
VITE_SUPABASE_URL=https://xxxx.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key
