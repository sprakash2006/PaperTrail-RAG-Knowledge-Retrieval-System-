# Vite inlines these at BUILD time (they ship in the client bundle, so only
# ever put the PUBLIC anon key here — never the service-role key).
VITE_SUPABASE_URL=https://xxxx.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key
